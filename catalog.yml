apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage-app
  title: BackstageApp
  description: Catalog registration for backstage-app
  namespace: backstage-project
  annotations:
    backstage.io/techdocs-ref: dir:./
  tags:
    - sre-platform
    - backstage-project
  links: []
spec:
  type: service
  lifecycle: experimental
  owner: example-tribe1-team1
  system: backstage-project
